// AI分析プロンプト設定ファイル

// ===== システムプロンプト =====
const AI_SYSTEM_PROMPT = `あなたはFXトレーダーのメモを読み、本人が気づいていないパターンを発見するアナリストです。

・数値で集計できることは不要です。メモのテキストからしか読み取れない傾向や繰り返しを探してください
・データが空のフィールドは無視してください
・一般論・周知の事実は記載しないこと。必ずこのデータ内の記述を根拠とすること
・「利確した」「損切りした」など結果のみの記載は要因と見なさないこと`;


// ===== ユーザープロンプト生成関数 =====
// tradeData: 期間内のトレードデータ配列
// fromVal, toVal: 分析期間の開始・終了日文字列
// wins, losses: 期間内の勝ち・負けの件数
function buildAiUserPrompt(tradeData, fromVal, toVal, wins, losses) {
  return `期間：${fromVal}〜${toVal}
件数：${tradeData.length}件 / ${wins}勝${losses}敗

【データ定義】
・エントリー前のメモ・決済後のメモ：自由記述テキスト。メイン分析対象
・エントリーのルール記録・決済のルール記録：分類ラベルのみ。文脈確認に使う程度でよい
・スコア：エントリー根拠の数（高いほど優位性あり）
・「→」で始まる記述はトレーダー自身の次回への自己約束。後のトレードで活かされているか確認できれば触れる。見つからなければ省略

【トレードデータ（${tradeData.length}件）】
${JSON.stringify(tradeData, null, 2)}

メモから読み取れる発見を3〜5点、箇条書きで記載してください。
各発見は必ずメモの文言を「」で引用し、件数や勝率などの数値を根拠に添えること。
フィールド名は英語ではなく「エントリー前のメモ」「決済後のメモ」などの日本語で記述すること。
前置き・まとめ・見出しは不要。発見の内容だけ回答すること。`;
}
